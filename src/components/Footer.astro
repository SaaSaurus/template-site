---
const {site} = Astro.props;
---

<section class="bg-secondary">
    <div class="max-w-screen-xl px-4 py-12 mx-auto space-y-8 overflow-hidden sm:px-6 lg:px-8">
        <!-- <nav class="flex flex-wrap justify-center -mx-5 -my-2">
            {site.footer.map((item) => (
                <div class="px-5 py-2">
                    <a href="{item.name}" class="text-base leading-6 text-gray-500 hover:text-gray-900 transition duration-300">
                        {item.name}
                    </a>
                </div>
            ))}
        </nav> -->
        <div class="flex justify-center mt-8 space-x-6">
            {site.socials.map((item) => (
                <a href="{item.link}" class="text-gray-400 hover:text-gray-500">
                    <img src="{item.icon}" class="h-6"/>
                </a>
            ))}
        </div>
        <p class="mt-8 text-base leading-6 text-center text-gray-400">
            © 2021 {site.title}, Inc. All rights reserved.
        </p>
    </div>
</section>